.col-md-2
  h2 Models
  .list-group
    each item in menu
      - var cls = item == path ? "list-group-item active" : "list-group-item"
      - var itemPath = item.toLowerCase()
      a(class=cls, href="#{base}/#{itemPath}")= item
